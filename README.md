### Swagger initialization Without docker

```
swag init
```

### Docker run image

```
docker build -t car-api .
```

### Docker run container

```
docker run -p 8080:8080 --name car-api car-api
```
